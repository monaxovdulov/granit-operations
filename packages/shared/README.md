# packages/shared

Placeholder for shared operations code.

Keep shared code operations-local. Public contract artifacts intended for `granit-site-cms` belong in `packages/contracts`, not in hidden application internals.

S01 currently contains stable hashing helpers for request fingerprinting.
